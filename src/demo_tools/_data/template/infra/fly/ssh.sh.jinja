#!/usr/bin/env bash
# MANAGED BY demo-tools — DO NOT EDIT. Run `just sync` to update.
set -euo pipefail
APP="{{ name }}"
{% if stack == "nextjs-fastapi" -%}
TARGET="${1:-web}"
fly ssh console --app "${APP}-${TARGET}"
{%- else -%}
fly ssh console --app "$APP"
{%- endif %}
