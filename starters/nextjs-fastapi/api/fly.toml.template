app = "__APP_API__"
primary_region = "lhr"

[build]

# Internal-only — no public http_service
[[services]]
  internal_port = 8000
  protocol = "tcp"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"

[mounts]
  source = "__VOLUME_NAME__"
  destination = "/data"
  initial_size = "1gb"
