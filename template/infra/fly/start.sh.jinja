#!/usr/bin/env bash
# MANAGED BY demo-tools — DO NOT EDIT. Run `just sync` to update.
set -euo pipefail
APP="{{ name }}"
{% if stack == "nextjs-fastapi" -%}
fly machines start --app "${APP}-api"
fly machines start --app "${APP}-web"
{%- else -%}
fly machines start --app "$APP"
{%- endif %}
echo "Started."
